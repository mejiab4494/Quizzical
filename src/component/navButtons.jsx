import { useState, useMemo } from "react";
import "./navButtons.css"


export default function NavButtons( {page, setPage}) {
    return (
        <div className="navBtn">
            <button onClick={()=> {}}> Refrigeration Cycle</button>
        </div>

    )
}